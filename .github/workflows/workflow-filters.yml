name: Workflow Filters and activity
on:
    workflow_dispatch: # Runs manually
    push:
        branches:
            - main
            - '!feature/*'
        # branches-ignore:
        #     - feature/*
        #     - test/**
    pull_request:
        types: [opened, synchronize, reopened]
        paths-ignore:
            - 'docs/**'
            - '**.md'
        branches:
            - main
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
        - name: Display Workflow Filters
          run: echo this workflow/job/step is executed for event type - ${{ github.event_name }} 
       